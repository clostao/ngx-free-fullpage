import { NgxFullpageToFirstButtonDirective } from './ngx-fullpage-to-first-button.directive';

describe('NgxFullpageToFirstButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxFullpageToFirstButtonDirective();
    expect(directive).toBeTruthy();
  });
});
