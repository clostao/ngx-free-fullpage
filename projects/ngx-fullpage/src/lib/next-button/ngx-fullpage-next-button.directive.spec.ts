import { NgxFullpageNextButtonDirective } from './ngx-fullpage-next-button.directive';

describe('NgxFullpageNextButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxFullpageNextButtonDirective();
    expect(directive).toBeTruthy();
  });
});

