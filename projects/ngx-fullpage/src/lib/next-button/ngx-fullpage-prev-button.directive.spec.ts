import { NgxFullpagePrevButtonDirective } from './ngx-fullpage-prev-button.directive';

describe('NgxFullpagePrevButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxFullpagePrevButtonDirective();
    expect(directive).toBeTruthy();
  });
});
