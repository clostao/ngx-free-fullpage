import { NgxFullpageWrapperDirective } from './ngx-fullpage-wrapper.directive';

describe('NgxFullpageWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxFullpageWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
