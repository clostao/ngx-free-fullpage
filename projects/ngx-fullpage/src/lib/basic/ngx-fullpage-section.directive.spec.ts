import { NgxFullpageSectionDirective } from './ngx-fullpage-section.directive';

describe('NgxFullpageSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxFullpageSectionDirective();
    expect(directive).toBeTruthy();
  });
});
